<Window x:Class="CybersecurityChatbotWPF.Views.QuizWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Quiz" 
        Height="550" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A2E"
        ResizeMode="CanResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#16213E" CornerRadius="10" Margin="5" Padding="15" BorderBrush="#0F3460" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Cybersecurity Quiz" Foreground="#00FFAA" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock x:Name="lblScore" Text="Score: 0 | Questions: 0" Foreground="#888888" FontSize="13" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#16213E" CornerRadius="10" Margin="5" Padding="20" BorderBrush="#0F3460" BorderThickness="1">
            <StackPanel>
                <TextBlock x:Name="lblQuestion" Text="Click 'Start Quiz' to begin!" Foreground="#FFFFFF" FontSize="16" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                <TextBlock x:Name="lblQuestionNumber" Text="" Foreground="#888888" FontSize="12" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#16213E" CornerRadius="10" Margin="5" Padding="10" BorderBrush="#0F3460" BorderThickness="1">
            <StackPanel x:Name="spAnswers" Margin="5"/>
        </Border>

        <Border Grid.Row="3" Background="#16213E" CornerRadius="10" Margin="5" Padding="15" BorderBrush="#0F3460" BorderThickness="1">
            <TextBlock x:Name="lblFeedback" Text="Ready to test your cybersecurity knowledge!" Foreground="#888888" FontSize="14" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="4" Background="#16213E" CornerRadius="10" Margin="5" Padding="10" BorderBrush="#0F3460" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="btnStartQuiz" Content="Start Quiz" Width="120" Height="35" Background="#00FFAA" Foreground="#1A1A2E" FontWeight="Bold" Click="BtnStartQuiz_Click" Margin="0,0,10,0"/>
                    <Button x:Name="btnNext" Content="Next Question" Width="140" Height="35" Background="#0F3460" Foreground="#FFFFFF" FontWeight="Bold" Click="BtnNext_Click" IsEnabled="False" Margin="5,0,0,0"/>
                </StackPanel>

                <Button x:Name="btnClose" Grid.Column="1" Content="Close" Width="80" Height="35" Background="#FF4444" Foreground="#FFFFFF" FontWeight="Bold" Click="BtnClose_Click" Margin="10,0,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>